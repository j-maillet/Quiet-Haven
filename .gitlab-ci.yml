pages:
  script:
    - mkdir public
    - cp index.html public/
    - cp -r Image styles.css public/ || echo "Pas d'image ou css"
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == "main"